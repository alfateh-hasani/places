# تعليمات إعداد خريطة المباني

## نظرة عامة
تم إضافة قسم خريطة تفاعلية يعرض جميع مواقع المباني في الصفحة الرئيسية للموقع.

## الملفات المضافة/المعدلة

### 1. الملفات الجديدة:
- `resources/views/home/parts/map.blade.php` - قالب عرض الخريطة
- `public/js/map.js` - ملف JavaScript للخريطة التفاعلية
- `MAP_SETUP_INSTRUCTIONS.md` - هذا الملف

### 2. الملفات المعدلة:
- `app/Http/Controllers/Front/HomeController.php` - إضافة جلب بيانات المباني
- `resources/views/home/index.blade.php` - إضافة قسم الخريطة
- `resources/lang/ar/site.php` - إضافة الترجمات العربية
- `resources/lang/en/site.php` - إضافة الترجمات الإنجليزية

## المتطلبات

### 1. Google Maps API Key
تحتاج إلى الحصول على مفتاح API من Google Maps:

1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. أنشئ مشروع جديد أو اختر مشروع موجود
3. فعّل Google Maps JavaScript API
4. أنشئ مفتاح API جديد
5. قم بتقييد المفتاح حسب الحاجة

### 2. تحديث مفتاح API
في الملف `public/js/map.js`، استبدل `YOUR_API_KEY` بمفتاح API الخاص بك:

```javascript
script.src = 'https://maps.googleapis.com/maps/api/js?key=YOUR_ACTUAL_API_KEY&callback=initBuildingMap&libraries=places';
```

## الميزات المضافة

### 1. خريطة تفاعلية
- عرض جميع المباني على الخريطة
- علامات مخصصة للمباني
- نوافذ معلومات تفاعلية
- تأثيرات بصرية عند التمرير

### 2. البحث في المباني
- شريط بحث للبحث في أسماء المباني والمدن
- بحث فوري أثناء الكتابة
- إمكانية مسح البحث

### 3. عرض معلومات المباني
- قائمة بجميع المباني تحت الخريطة
- معلومات تفصيلية لكل مبنى
- روابط مباشرة لصفحات المباني

### 4. تصميم متجاوب
- يعمل على جميع الأجهزة
- تصميم متوافق مع الهواتف المحمولة
- ألوان متناسقة مع تصميم الموقع

## البيانات المطلوبة

تأكد من أن جدول `buildings` يحتوي على:
- `latitude` - خط العرض
- `longitude` - خط الطول
- `name_ar` - الاسم بالعربية
- `name_en` - الاسم بالإنجليزية
- `slug` - الرابط المختصر
- `city_id` - معرف المدينة

## التخصيص

### 1. تغيير الألوان
في ملف `resources/views/home/parts/map.blade.php`، يمكنك تغيير الألوان:

```css
.bg-primary {
    background-color: #your-color;
}
```

### 2. تغيير الإحداثيات الافتراضية
في ملف `public/js/map.js`:

```javascript
this.defaultCenter = { lat: YOUR_LAT, lng: YOUR_LNG };
```

### 3. إضافة ميزات جديدة
يمكنك إضافة ميزات جديدة في فئة `BuildingMap` مثل:
- تصفية حسب المدينة
- عرض معلومات إضافية
- ربط مع نظام الحجز

## استكشاف الأخطاء

### 1. الخريطة لا تظهر
- تأكد من صحة مفتاح Google Maps API
- تحقق من وجود أخطاء في وحدة تحكم المتصفح
- تأكد من وجود بيانات المباني مع الإحداثيات

### 2. العلامات لا تظهر
- تأكد من وجود قيم `latitude` و `longitude` في قاعدة البيانات
- تحقق من صحة البيانات المرسلة للـ JavaScript

### 3. البحث لا يعمل
- تأكد من تحميل ملف `map.js` بشكل صحيح
- تحقق من وجود عنصر البحث في الصفحة

## الأمان

1. قم بتقييد مفتاح Google Maps API حسب النطاق
2. لا تشارك مفتاح API في الكود العام
3. راقب استخدام API لتجنب تجاوز الحدود

## الدعم

للحصول على المساعدة:
1. راجع وثائق Google Maps JavaScript API
2. تحقق من وحدة تحكم المتصفح للأخطاء
3. تأكد من صحة البيانات في قاعدة البيانات

## التحديثات المستقبلية

يمكن إضافة الميزات التالية:
- تصفية المباني حسب المدينة
- عرض معلومات الأسعار
- ربط مع نظام الحجز المباشر
- إضافة صور للمباني في العلامات
- دعم الوضع الليلي للخريطة
